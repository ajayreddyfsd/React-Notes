// Writing a simple functional component

// Using that component as a tag in JSX

// Passing props as inputs via attributes

// handling props inside the component

// also the components folder shd be inside the src folder
